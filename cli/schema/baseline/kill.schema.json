{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.curietech.ai/cli/kill/v1.json",
  "title": "curie <tier> kill --json",
  "description": "The agent-facing payload of `curie <tier> kill <agent>`: the dry-run plan, or the resulting kill state.",
  "$defs": {
    "dryRun": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "dry_run": {
          "const": true
        },
        "plan": {
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      },
      "required": [
        "dry_run",
        "plan"
      ]
    }
  },
  "oneOf": [
    {
      "$ref": "#/$defs/dryRun"
    },
    {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "agent": {
          "type": "string"
        },
        "killed": {
          "type": "boolean"
        }
      },
      "required": [
        "agent",
        "killed"
      ]
    }
  ]
}
